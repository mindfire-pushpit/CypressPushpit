

describe('One Way Trip test suite',()=>{

    it('Jaipur to lucknow one way ticket',()=>{

        cy.visit('https://www.makemytrip.com/');
        // cy.title().should('contain','Yatra');
        // cy.get('a[title="One Way"]').should('be.focused');
        
    })
})